# macOS em volume externo (AppleDouble / resource fork) — nunca versionar
._*
**/._*
**/.DS_Store

# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/

# Native (gerados pelo prebuild local; EAS gera de novo na nuvem)
android/
ios/

# Env / OS
.env
.env.*
.DS_Store
*.log
npm-debug.log*

# IDE
.idea/
.vscode/

# Scripts de manutencao com credenciais de banco — NUNCA versionar
insert_driver.js
insert_driver_paratinga.js
check_rides.js
fix_driver.js
cancel_stuck_rides.php
limpar_corridas.js

# Chave de upload Android (Play) e credenciais locais — NUNCA versionar
/credentials.json
credentials/*.jks
credentials/*.keystore
credentials/*.pem
